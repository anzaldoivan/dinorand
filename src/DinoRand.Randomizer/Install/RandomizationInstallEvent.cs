namespace DinoRand.Randomizer.Install;

/// <summary>A user-facing message emitted during a coordinated randomization install.</summary>
internal sealed record RandomizationInstallEvent(string Message);
